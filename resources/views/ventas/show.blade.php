<x-layouts.app :title="__('Detalle de venta')"><div class="min-h-full bg-[#fffafc] p-4 sm:p-8 dark:bg-zinc-900"><div class="mx-auto max-w-3xl"><a href="{{ route('ventas.index') }}" class="text-sm text-[#b34c6d]">&#8592; Volver a ventas</a><div class="mt-4 rounded-2xl bg-white p-6 shadow-sm dark:bg-zinc-800"><div class="flex flex-col justify-between gap-2 sm:flex-row"><div><h1 class="mt-1 text-2xl font-semibold">{{ $venta->cliente ?: 'Consumidor final' }}</h1></div><p class="text-2xl font-semibold text-[#b34c6d]">Q{{ number_format($venta->total, 2) }}</p></div><p class="mt-2 text-sm text-zinc-500">{{ $venta->fecha->format('d/m/Y') }}</p><div class="mt-6 divide-y">@foreach ($venta->detalles as $detalle)<div class="flex justify-between py-3 text-sm"><span>{{ $detalle->cantidad }} x {{ $detalle->producto->nombre }}</span><span class="font-medium">Q{{ number_format($detalle->subtotal, 2) }}</span></div>@endforeach</div><p class="mt-5 text-sm text-zinc-500">{{ $venta->observaciones }}</p></div></div></div></x-layouts.app>
